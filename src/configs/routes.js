const APP_BASE_URL = "";

const APP_ROUTES = {
  HOME: `${APP_BASE_URL}/home`,
  CREATE_TICKET: `${APP_BASE_URL}/create-ticket`,
  STRIPE_GATEWAY: `${APP_BASE_URL}/payment`,
};

export { APP_BASE_URL, APP_ROUTES };
